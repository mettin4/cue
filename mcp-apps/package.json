{
  "name": "cue-mcp-apps",
  "private": true,
  "type": "module",
  "description": "Inline MCP Apps UI resources for Cue. Built to a single self-contained HTML file that the remote MCP endpoint serves.",
  "scripts": {
    "build": "vite build && node scripts/emit.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.0",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
